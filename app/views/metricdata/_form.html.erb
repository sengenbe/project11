<%= form_for(@metricdatum) do |f| %>
  <% if @metricdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metricdatum.errors.count, "error") %> prohibited this metricdatum from being saved:</h2>

      <ul>
      <% @metricdatum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :adddate %><br />
    <%= f.datetime_select :adddate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
